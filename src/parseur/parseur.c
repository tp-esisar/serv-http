#include "parseur.h"
#include "reader.h"



int parser (char *buf,unsigned int len,char *search, void (*callback)()) {
  
  return -1;
}

parse_result parse_HTTP_message(StringL* buff, HTTP_message* HTTP_message) {
    
    
}
