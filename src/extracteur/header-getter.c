#include "header-getter.h"


// Host_HS* get_Host(mapStruct* map) {
    
//     static Host_HS* cache = NULL;
//     if(cache != NULL) {
//         return cache;
//     }
    
    
//     search_map (mapStruct* map, char* search, void (*callback)(char* found, unsigned int len));
    
    
//     return ret;
// }